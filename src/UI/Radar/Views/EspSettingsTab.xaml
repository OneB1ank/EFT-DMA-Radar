<UserControl x:Class="LoneEftDmaRadar.UI.Radar.Views.EspSettingsTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:LoneEftDmaRadar.UI.Radar.Views"
             xmlns:conv="clr-namespace:LoneEftDmaRadar.UI.Misc"
             xmlns:vm="clr-namespace:LoneEftDmaRadar.UI.Radar.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             mc:Ignorable="d"
             Padding="10">

    <d:UserControl.DataContext>
        <d:DesignInstance Type="{x:Type vm:SettingsViewModel}"
                          IsDesignTimeCreatable="False" />
    </d:UserControl.DataContext>

    <UserControl.Resources>
        <conv:StringToColorConverter x:Key="StringToColorConverter" />
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Orientation="Vertical"
                    Margin="0,0,0,10">

            <!-- General Settings -->
            <GroupBox Header="ESP �ں� - ͨ������"
                      Margin="0,0,0,12">
                <StackPanel>
                    <WrapPanel>
                        <Button Content="����ں�" 
                                Command="{Binding StartEspCommand}"
                                Width="120"
                                Height="32"
                                Margin="0,0,16,8"
                                ToolTip="Opens the ESP Window in Fullscreen Mode."/>
                        <CheckBox Content="Auto Startup"
                                  IsChecked="{Binding EspAutoStartup, Mode=TwoWay}"
                                  VerticalAlignment="Center"
                                  ToolTip="Automatically start ESP Fuser when the application launches."/>
                    </WrapPanel>

                    <WrapPanel Margin="0,4,0,0">
                        <TextBlock Text="Ŀ����ʾ����" VerticalAlignment="Center" Margin="0,0,8,0" Width="100"/>
                        <ComboBox Width="300"
                                  ItemsSource="{Binding AvailableScreens}"
                                  SelectedValue="{Binding EspTargetScreen, Mode=TwoWay}"
                                  SelectedValuePath="Index"
                                  DisplayMemberPath="DisplayName"
                                  ToolTip="��ʼ�ں�ʱѡ��Ҫ��������ʾ ESP ���ڵ���ʾ����"/>
                    </WrapPanel>

                    <WrapPanel Margin="0,4,0,0">
                        <TextBlock Text="�ֱ��ʸ��� (0 = �Զ�)�� " VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBox Width="65" Text="{Binding EspScreenWidth, Mode=TwoWay}" Margin="0,0,8,0" ToolTip="��ȣ����� 1920��2560��������Ϊ 0 ���Զ������ѡ��ʾ���ֱ��ʡ�"/>
                        <TextBlock Text="x" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBox Width="65" Text="{Binding EspScreenHeight, Mode=TwoWay}" Margin="0,0,16,0" ToolTip="�߶ȣ����� 1080��1440��������Ϊ 0 ���Զ������ѡ��ʾ���ֱ��ʡ�"/>
                    </WrapPanel>

                    <GroupBox Header="������ʽ" Margin="0,8,0,0">
                        <StackPanel>
                            <WrapPanel Margin="0,4,0,0">
                                <TextBlock Text="���壺" VerticalAlignment="Center" Width="90"/>
                                <TextBox Width="180"
                                         Text="{Binding EspFontFamily, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                         ToolTip="ʹ���κΰ�װ���������ƣ����� Segoe UI��Arial��Consolas��"/>
                            </WrapPanel>
                            <WrapPanel Margin="0,4,0,0">
                                <TextBlock Text="�ֺ� (S/M/L)��" VerticalAlignment="Center" Width="90"/>
                                <TextBox Width="50"
                                         Text="{Binding EspFontSizeSmall, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                         ToolTip="С�ֺţ������ǩ��"/>
                                <TextBlock Text="/" VerticalAlignment="Center" Margin="6,0"/>
                                <TextBox Width="50"
                                         Text="{Binding EspFontSizeMedium, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                         ToolTip="���ֺţ����֡�����㣩"/>
                                <TextBlock Text="/" VerticalAlignment="Center" Margin="6,0"/>
                                <TextBox Width="50"
                                         Text="{Binding EspFontSizeLarge, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                         ToolTip="���ֺţ�״̬�ı���"/>
                            </WrapPanel>
                            <TextBlock Text="���ĻἴʱӦ�õ� ESP ���ǲ�"
                                       Foreground="Gray"
                                       FontSize="10"
                                       Margin="0,4,0,0"/>
                        </StackPanel>
                    </GroupBox>
                </StackPanel>
            </GroupBox>

            <GroupBox Header="��� ESP" Margin="0,0,0,12">
                <StackPanel>
                    <WrapPanel>
                        <CheckBox Content="����" IsChecked="{Binding EspPlayerBoxes, Mode=TwoWay}" Margin="0,0,16,8" />
                        <CheckBox Content="�Ǽ�" IsChecked="{Binding EspPlayerSkeletons, Mode=TwoWay}" Margin="0,0,16,8" />
                        <CheckBox Content="����" IsChecked="{Binding EspPlayerNames, Mode=TwoWay}" Margin="0,0,16,8" />
                        <CheckBox Content="���� ID" IsChecked="{Binding EspGroupIds, Mode=TwoWay}" Margin="0,0,16,8" />
                        <CheckBox Content="������ɫ" IsChecked="{Binding EspGroupColors, Mode=TwoWay}" Margin="0,0,16,8" />
                        <CheckBox Content="��Ӫ����" IsChecked="{Binding EspPlayerFaction, Mode=TwoWay}" Margin="0,0,16,8" />
                        <CheckBox Content="��Ӫ��ɫ" IsChecked="{Binding EspFactionColors, Mode=TwoWay}" Margin="0,0,16,8" />
                        <CheckBox Content="Ѫ��״̬" IsChecked="{Binding EspPlayerHealth, Mode=TwoWay}" Margin="0,0,16,8" />
                        <CheckBox Content="����" IsChecked="{Binding EspPlayerDistance, Mode=TwoWay}" Margin="0,0,16,8" />
                        <CheckBox Content="ͷ��ԲȦ" IsChecked="{Binding EspHeadCirclePlayers, Mode=TwoWay}" Margin="0,0,16,8" />
                     </WrapPanel>
                     <WrapPanel Margin="0,4,0,0">
                        <TextBlock Text="������ (0 = ����)��" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <Slider x:Name="PlayerDistanceSlider"
                                Width="300"
                                Margin="0,0,8,0"
                                Minimum="0"
                                Maximum="1000"
                                SmallChange="10"
                                LargeChange="100"
                                Value="{Binding EspPlayerMaxDistance, Mode=TwoWay}"/>
                        <TextBlock Text="{Binding Value, ElementName=PlayerDistanceSlider, StringFormat='{}{0:F0}m'}"
                                   VerticalAlignment="Center"
                                   MinWidth="50"/>
                    </WrapPanel>
                    <WrapPanel Margin="0,8,0,0">
                        <TextBlock Text="��ǩλ�ã���ң���" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <ComboBox Width="140"
                                  ItemsSource="{Binding LabelPositions}"
                                  SelectedItem="{Binding EspLabelPosition, Mode=TwoWay}"
                                  ToolTip="ѡ����ұ�ǩ����ڰ�Χ�еĻ���λ�á�"/>
                        <StackPanel Orientation="Horizontal" Margin="16,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="��ɫ��" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <xctk:ColorPicker Width="42" Height="26"
                                              SelectedColor="{Binding EspColorPlayers, Mode=TwoWay, Converter={StaticResource StringToColorConverter}}"
                                              UsingAlphaChannel="True"
                                              ShowRecentColors="False"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="16,4,0,0" VerticalAlignment="Center">
                            <TextBlock Text="BEAR ��Ӫ��" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <xctk:ColorPicker Width="42" Height="26"
                                              SelectedColor="{Binding EspColorFactionBear, Mode=TwoWay, Converter={StaticResource StringToColorConverter}}"
                                              UsingAlphaChannel="True"
                                              ShowRecentColors="False"/>
                            <TextBlock Text="USEC��" VerticalAlignment="Center" Margin="10,0,6,0"/>
                            <xctk:ColorPicker Width="42" Height="26"
                                              SelectedColor="{Binding EspColorFactionUsec, Mode=TwoWay, Converter={StaticResource StringToColorConverter}}"
                                              UsingAlphaChannel="True"
                                              ShowRecentColors="False"/>
                        </StackPanel>
                    </WrapPanel>
                </StackPanel>
            </GroupBox>
            
            <GroupBox Header="AI ESP" Margin="0,0,0,12">
                <StackPanel>
                    <WrapPanel>
                        <CheckBox Content="����" IsChecked="{Binding EspAIBoxes, Mode=TwoWay}" Margin="0,0,16,8" />
                        <CheckBox Content="�Ǽ�" IsChecked="{Binding EspAISkeletons, Mode=TwoWay}" Margin="0,0,16,8" />
                        <CheckBox Content="����" IsChecked="{Binding EspAINames, Mode=TwoWay}" Margin="0,0,16,8" />
                        <CheckBox Content="Ѫ��״̬" IsChecked="{Binding EspAIHealth, Mode=TwoWay}" Margin="0,0,16,8" />
                        <CheckBox Content="����" IsChecked="{Binding EspAIDistance, Mode=TwoWay}" Margin="0,0,16,8" />
                        <CheckBox Content="ͷ��ԲȦ" IsChecked="{Binding EspHeadCircleAI, Mode=TwoWay}" Margin="0,0,16,8" />
                     </WrapPanel>
                    <WrapPanel Margin="0,8,0,0">
                        <TextBlock Text="��ǩλ�ã�AI/Scav����" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <ComboBox Width="140"
                                  ItemsSource="{Binding LabelPositions}"
                                  SelectedItem="{Binding EspLabelPositionAI, Mode=TwoWay}"
                                  ToolTip="ѡ�� AI/scav ��ǩ����ڰ�Χ�еĻ���λ�á�"/>
                        <StackPanel Orientation="Horizontal" Margin="16,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="AI/Scav ��ɫ��" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <xctk:ColorPicker Width="42" Height="26"
                                              SelectedColor="{Binding EspColorAI, Mode=TwoWay, Converter={StaticResource StringToColorConverter}}"
                                              UsingAlphaChannel="True"
                                              ShowRecentColors="False"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="16,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="��� Scav ��ɫ��" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <xctk:ColorPicker Width="42" Height="26"
                                              SelectedColor="{Binding EspColorPlayerScavs, Mode=TwoWay, Converter={StaticResource StringToColorConverter}}"
                                              UsingAlphaChannel="True"
                                              ShowRecentColors="False"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="16,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="�Ӷ��� ��ɫ��" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <xctk:ColorPicker Width="42" Height="26"
                                              SelectedColor="{Binding EspColorRaiders, Mode=TwoWay, Converter={StaticResource StringToColorConverter}}"
                                              UsingAlphaChannel="True"
                                              ShowRecentColors="False"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="16,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="Boss ��ɫ��" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <xctk:ColorPicker Width="42" Height="26"
                                              SelectedColor="{Binding EspColorBosses, Mode=TwoWay, Converter={StaticResource StringToColorConverter}}"
                                              UsingAlphaChannel="True"
                                              ShowRecentColors="False"/>
                        </StackPanel>
                     </WrapPanel>
                     <WrapPanel Margin="0,4,0,0">
                        <TextBlock Text="������ (0 = ����)��" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <Slider x:Name="AIDistanceSlider"
                                Width="300"
                                Margin="0,0,8,0"
                                Minimum="0"
                                Maximum="1000"
                                SmallChange="10"
                                LargeChange="100"
                                Value="{Binding EspAIMaxDistance, Mode=TwoWay}"/>
                        <TextBlock Text="{Binding Value, ElementName=AIDistanceSlider, StringFormat='{}{0:F0}m'}"
                                   VerticalAlignment="Center"
                                   MinWidth="50"/>
                     </WrapPanel>
                    <TextBlock Text="ͷ��ԲȦ��̳�ʵ����ɫ����ҡ�Scav���Ӷ��ߡ�Boss����"
                               Foreground="Gray"
                               FontSize="10"
                               Margin="0,8,0,0"/>
                </StackPanel>
            </GroupBox>
            
            <GroupBox Header="���䡢������뱬ը��" Margin="0,0,0,12">
                <StackPanel>
                     <WrapPanel>
                        <CheckBox Content="��ʾ����" IsChecked="{Binding EspLoot, Mode=TwoWay}" Margin="0,0,16,8" />
                        <CheckBox Content="��ʾ�۸�" IsChecked="{Binding EspLootPrice, Mode=TwoWay}" Margin="0,0,16,8" IsEnabled="{Binding EspLoot}" />
                        <CheckBox Content="��ʾ�����" IsChecked="{Binding EspExfils, Mode=TwoWay}" Margin="0,0,16,8" />
                        <CheckBox Content="��ʾ����" IsChecked="{Binding EspTripwires, Mode=TwoWay}" Margin="0,0,16,8" />
                        <CheckBox Content="��ʾ����" IsChecked="{Binding EspGrenades, Mode=TwoWay}" Margin="0,0,16,8" />
                        <CheckBox Content="����׶������"
                                  IsChecked="{Binding EspLootConeEnabled, Mode=TwoWay}"
                                  Margin="0,0,16,8" />
                     </WrapPanel>
                     <WrapPanel Margin="0,4,0,0">
                        <CheckBox Content="��ʾʳ��" IsChecked="{Binding EspFood, Mode=TwoWay}" Margin="0,0,16,8" />
                        <CheckBox Content="��ʾҩƷ" IsChecked="{Binding EspMeds, Mode=TwoWay}" Margin="0,0,16,8" />
                        <CheckBox Content="��ʾ����" IsChecked="{Binding EspBackpacks, Mode=TwoWay}" Margin="0,0,16,8" />
                        <CheckBox Content="��ʾ������Ʒ" IsChecked="{Binding EspQuestLoot, Mode=TwoWay}" Margin="0,0,16,8" />
                        <CheckBox Content="��ʾʬ��" IsChecked="{Binding EspCorpses, Mode=TwoWay}" Margin="0,0,16,8" />
                        <CheckBox Content="��ʾ����" IsChecked="{Binding EspContainers, Mode=TwoWay}" Margin="0,0,16,8" />
                     </WrapPanel>
                     <WrapPanel Margin="0,4,0,0" IsEnabled="{Binding EspContainers}">
                        <TextBlock Text="�������� (ESP)"
                                   VerticalAlignment="Center" />
                        <Slider x:Name="EspContainerDistanceSlider"
                                Width="150"
                                Margin="8,0"
                                Minimum="0"
                                Maximum="200"
                                SmallChange="1"
                                LargeChange="10"
                                Value="{Binding EspContainerDistance, Mode=TwoWay}" />
                        <TextBlock Text="{Binding Value, ElementName=EspContainerDistanceSlider, StringFormat='{}{0:F0}'}"
                                   VerticalAlignment="Center"
                                   Margin="8,0,0,0" />
                     </WrapPanel>
                     <WrapPanel Margin="0,4,0,0" IsEnabled="{Binding EspLootConeEnabled}">
                        <TextBlock Text="׶�νǶ�"
                                   VerticalAlignment="Center" />
                        <Slider x:Name="LootConeSlider"
                                Width="150"
                                Margin="8,0"
                                Minimum="5"
                                Maximum="60"
                                SmallChange="1"
                                LargeChange="5"
                                Value="{Binding EspLootConeAngle, Mode=TwoWay}" />
                        <TextBlock Text="{Binding Value, ElementName=LootConeSlider, StringFormat=F0}"
                                   VerticalAlignment="Center"
                                   Margin="8,0,0,0" />
                     </WrapPanel>
                    <WrapPanel Margin="0,4,0,0">
                        <TextBlock Text="������ (0 = ����)��" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <Slider x:Name="LootDistanceSlider"
                                Width="200"
                                Margin="0,0,8,0"
                                Minimum="0"
                                Maximum="500"
                                SmallChange="10"
                                LargeChange="50"
                                Value="{Binding EspLootMaxDistance, Mode=TwoWay}"/>
                        <TextBlock Text="{Binding Value, ElementName=LootDistanceSlider, StringFormat='{}{0:F0}m'}"
                                   VerticalAlignment="Center"
                                   MinWidth="50"/>
                        <StackPanel Orientation="Horizontal" Margin="16,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="������ɫ��" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <xctk:ColorPicker Width="42" Height="26"
                                              SelectedColor="{Binding EspColorLoot, Mode=TwoWay, Converter={StaticResource StringToColorConverter}}"
                                              UsingAlphaChannel="True"
                                              ShowRecentColors="False"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="16,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="������ɫ��" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <xctk:ColorPicker Width="42" Height="26"
                                              SelectedColor="{Binding EspColorContainers, Mode=TwoWay, Converter={StaticResource StringToColorConverter}}"
                                              UsingAlphaChannel="True"
                                              ShowRecentColors="False"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="16,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="�������ɫ��" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <xctk:ColorPicker Width="42" Height="26"
                                              SelectedColor="{Binding EspColorExfil, Mode=TwoWay, Converter={StaticResource StringToColorConverter}}"
                                              UsingAlphaChannel="True"
                                              ShowRecentColors="False"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="16,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="������ɫ��" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <xctk:ColorPicker Width="42" Height="26"
                                              SelectedColor="{Binding EspColorTripwire, Mode=TwoWay, Converter={StaticResource StringToColorConverter}}"
                                              UsingAlphaChannel="True"
                                              ShowRecentColors="False"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="16,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="������ɫ��" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <xctk:ColorPicker Width="42" Height="26"
                                              SelectedColor="{Binding EspColorGrenade, Mode=TwoWay, Converter={StaticResource StringToColorConverter}}"
                                              UsingAlphaChannel="True"
                                              ShowRecentColors="False"/>
                        </StackPanel>
                    </WrapPanel>
                </StackPanel>
            </GroupBox>

            <!-- Mini Radar Settings -->
            <GroupBox Header="�����״�" Margin="0,0,0,12">
                <StackPanel>
                    <CheckBox Content="���������״�" IsChecked="{Binding MiniRadarEnabled, Mode=TwoWay}" Margin="0,0,0,8"/>
                    
                    <WrapPanel Margin="0,0,0,8" IsEnabled="{Binding MiniRadarEnabled}">
                         <TextBlock Text="��С��" VerticalAlignment="Center" Width="60"/>
                         <Slider Width="150" Minimum="150" Maximum="512" Value="{Binding MiniRadarSize, Mode=TwoWay}" SmallChange="10" LargeChange="50"/>
                         <TextBlock Text="{Binding MiniRadarSize, StringFormat='{}{0} px'}" VerticalAlignment="Center" Margin="8,0,0,0"/>
                    </WrapPanel>

                    <WrapPanel Margin="0,0,0,8" IsEnabled="{Binding MiniRadarEnabled}">
                        <TextBlock Text="λ�ã�" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="X" VerticalAlignment="Center" Margin="0,0,4,0"/>
                        <TextBox Width="50" Text="{Binding MiniRadarScreenX, Mode=TwoWay}" ToolTip="-1 = �Զ����ң�"/>
                        <TextBlock Text="Y" VerticalAlignment="Center" Margin="8,0,4,0"/>
                        <TextBox Width="50" Text="{Binding MiniRadarScreenY, Mode=TwoWay}"/>
                        <TextBlock Text="(-1 X = �Զ� ����)" VerticalAlignment="Center" Margin="8,0,0,0" Foreground="Gray" FontSize="10"/>
                    </WrapPanel>

                    <TextBlock Text="�ɼ�ͼ��" FontWeight="SemiBold" Margin="0,4,0,4" IsEnabled="{Binding MiniRadarEnabled}"/>
                    <WrapPanel IsEnabled="{Binding MiniRadarEnabled}">
                         <CheckBox Content="���" IsChecked="{Binding MiniRadarShowPlayers, Mode=TwoWay}" Margin="0,0,12,0"/>
                         <CheckBox Content="����" IsChecked="{Binding MiniRadarShowLoot, Mode=TwoWay}" Margin="0,0,12,0"/>
                         <CheckBox Content="����" IsChecked="{Binding MiniRadarShowContainers, Mode=TwoWay}" Margin="0,0,12,0"/>
                         <CheckBox Content="����" IsChecked="{Binding MiniRadarShowExits, Mode=TwoWay}" Margin="0,0,12,0"/>
                         <CheckBox Content="����" IsChecked="{Binding MiniRadarShowGrenades, Mode=TwoWay}" Margin="0,0,12,0"/>
                    </WrapPanel>
                    <CheckBox Content="��ת��ͼ��ɫ����ɫ��" IsChecked="{Binding MiniRadarInvertColors, Mode=TwoWay}" IsEnabled="{Binding MiniRadarEnabled}" Margin="0,8,0,0"/>
                </StackPanel>
            </GroupBox>

            <!-- Performance Settings -->
            <GroupBox Header="����"
                      Margin="0,0,0,12">
                <StackPanel>
                    <StackPanel Margin="0,0,0,8">
                        <TextBlock Text="ESP ��� FPS (0 = ���� / Ĭ�� 144)��"
                                   VerticalAlignment="Center" />
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                            <Slider x:Name="EspFpsSlider"
                                    Width="220"
                                    Minimum="0"
                                    Maximum="360"
                                    SmallChange="10"
                                    LargeChange="30"
                                    TickFrequency="30"
                                    IsSnapToTickEnabled="True"
                                   Value="{Binding EspMaxFPS, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBlock Text="{Binding Value, ElementName=EspFpsSlider, StringFormat='{}{0:F0}'}"
                                       VerticalAlignment="Center"
                                       Margin="8,0,0,0" />
                        </StackPanel>
                        <TextBlock Text="0 = ���ޣ�Ĭ��Ϊ 144�����ϵ�ֵ�ɽ�ʡ CPU/GPU��"
                                   Foreground="Gray"
                                   FontSize="10"
                                   Margin="0,2,0,0" />
                    </StackPanel>


                    <CheckBox Content="׼��"
                                  ToolTip="�� ESP ����������ʾһ���򵥵�׼�ǡ�"
                                  IsChecked="{Binding EspCrosshair, Mode=TwoWay}"
                                  Margin="0,0,16,8" />

                    <WrapPanel Margin="0,4,0,0" IsEnabled="{Binding EspCrosshair}">
                        <TextBlock Text="׼�ǳ���" VerticalAlignment="Center"/>
                        <Slider x:Name="CrosshairLengthSlider"
                                Width="150"
                                Margin="8,0"
                                Minimum="5"
                                Maximum="100"
                                SmallChange="1"
                                LargeChange="5"
                                Value="{Binding EspCrosshairLength, Mode=TwoWay}"/>
                        <TextBlock Text="{Binding Value, ElementName=CrosshairLengthSlider, StringFormat=F0}"
                                   VerticalAlignment="Center"
                                   Margin="8,0,0,0"/>
                    </WrapPanel>

                    <StackPanel Orientation="Horizontal" Margin="16,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="׼����ɫ��" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <xctk:ColorPicker Width="42" Height="26"
                                              SelectedColor="{Binding EspColorCrosshair, Mode=TwoWay, Converter={StaticResource StringToColorConverter}}"
                                              UsingAlphaChannel="True"
                                              ShowRecentColors="False"/>
                    </StackPanel>
                </StackPanel>
            </GroupBox>

        </StackPanel>
    </ScrollViewer>
</UserControl>
